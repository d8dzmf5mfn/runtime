{
  "mcpServers": {
    "runtime": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PROJECT_DIR}/packages/mcp-server/dist/index.js"],
      "env": { "VSCODE_WORKSPACE_ROOT": "${CLAUDE_PROJECT_DIR}" }
    }
  }
}
